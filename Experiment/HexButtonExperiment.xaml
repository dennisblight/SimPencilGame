<Window x:Class="Experiment.HexButtonExperiment"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Experiment"
        mc:Ignorable="d"
        Title="HexButtonExperiment" Height="300" Width="300">
    <Grid>
        <local:HexButton AccentColor="{StaticResource Color01}" Height="50" Width="200" Glyph="{StaticResource GlyphQuestion}" Text="Play!"/>

        <!--
        <Button Style="{StaticResource newTemplate}" 
          Content="Button1">
        </Button>
        <Polygon Fill="Red" Stretch="Fill" Width="20" Height="20">
            <Polygon.RenderTransform>
                <ScaleTransform ScaleX="{Binding Width, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Polygon}}}"
                                ScaleY="{Binding Height, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Polygon}}}"/>
            </Polygon.RenderTransform>
            <Polygon.Points>
                <Point X="0" Y="0"/>
                <Point X="1" Y="0"/>
                <Point X="1" Y="1"/>
                <Point X="0" Y="1"/>
            </Polygon.Points>
        </Polygon>
        -->


    </Grid>
</Window>
